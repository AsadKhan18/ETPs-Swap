//imported from the components we created
import {SwapBodyContent} from "../Components/index";

const Pool = ({Component, pageProps}) => {
    return (
        <div className="pt-16">
            <SwapBodyContent></SwapBodyContent>
        </div>
    );
};

export default Pool;
